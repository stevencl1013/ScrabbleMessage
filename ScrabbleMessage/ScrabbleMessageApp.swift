import SwiftUI

@main
struct ScrabbleMessageApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Open this app in iMessage")
                .font(.headline)
                .foregroundColor(.secondary)
        }
    }
}
